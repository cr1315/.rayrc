### Confirm Architecture Before Coding
Before making any changes, propose your approach as a numbered plan. Include: which files you'll modify, what the key design decision is, and any assumptions you're making about the platform/environment. Wait for my approval before proceeding.

### Use Docker for Shell Script Validation
Before applying these shell script changes, create a minimal Dockerfile that tests the script in a clean environment matching our target (Alpine for OpenWRT-like, ubuntu:latest for general). Run the test and show me the output before we proceed.
